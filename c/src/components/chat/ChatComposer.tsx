import { useRef } from 'react'; import { useI18n } from '../../context/I18nContext'; export function ChatComposer({value,onChange,onSend,onStop,disabled,active}:{value:string;onChange:(v:string)=>void;onSend:()=>void;onStop:()=>void;disabled:boolean;active:boolean}){const { t } = useI18n(); const composing=useRef(false);const sendDisabled=disabled||!value.trim()||active;return <div className="chat-composer"><textarea rows={1} value={value} disabled={disabled||active} onChange={e=>onChange(e.target.value)} onCompositionStart={()=>{composing.current=true}} onCompositionEnd={()=>{composing.current=false}} onKeyDown={e=>{if(e.key==='Enter'&&!e.shiftKey&&!composing.current&&!sendDisabled){e.preventDefault();onSend()}}} placeholder={t('chat_message_placeholder')} aria-label={t('chat_message_placeholder')}/><button className="button button-primary" disabled={active?false:sendDisabled} onClick={active?onStop:onSend}>{active?t('chat_stop'):t('chat_send')}</button></div>}
