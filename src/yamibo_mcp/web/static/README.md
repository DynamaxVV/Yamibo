# Generated Static Artifact

This file is copied into `src/yamibo_mcp/web/static/` during `npm --prefix frontend run build`.

Editable web UI source lives in `/frontend`. The packaged artifact served by `yamibo_mcp.web.app` lives in `src/yamibo_mcp/web/static/`.

The hashed files under `assets/` are generated/minified artifacts. Do not edit or inspect them for application logic unless the task is specifically about static routing, packaging, or cache busting.
