# 本地讨论研究

适用：用户要求基于已归档讨论作总结、比较或找证据。

1. 在 Run 冻结范围内用 `find_discussions` 找候选；它只覆盖讨论范围，不能替代漫画区或远端论坛搜索。
2. 对要引用的楼层用 `read_discussion_source` 阅读原文，最终引用前用 `validate_discussion_citations` 校验回执。
3. 区分候选、已读原文、统计结果和推断；范围不足时说明覆盖缺口，不将片段说成全帖。
4. 需要趋势或报告时先核对 PostgreSQL 趋势索引状态；创建索引和报告是后台 Job，等待其终态后读取产物。
