# Design — Yamibo Web 设计契约

## Source of truth

- **状态**：Active (v2.1 已实施；以当前视觉复验记录中的范围和限制为准)
- **最近更新**：2026-09-23
- **设计参考**：`ui渲染.png` 提供纸墨色彩与出版物排版气质，不规定页面信息或功能标签。2026-09-23 本地桌面与 390×844 页面检查及 `docs/Web-UI-v2.1-设计与执行方案.md` 提供问题证据、页面规格和验收顺序。
- **技术实现依据**：Tailwind CSS v4、shadcn/ui 设计原语、`motion`（物理弹簧动效）、`@tanstack/react-query`、`lucide-react`、`c/src/index.css`、`c/src/components/Layout.tsx`、`c/src/pages/Dashboard.tsx`、`c/src/components/ThreadReader.tsx`。
- **本文件定位**：全站 UI/UX 视觉、布局、动效与排版决策的权威依据；具体组件与业务逻辑以源码为准。
- **决策顺序**：本文件的 v2.1 规则优先于 `docs/Web界面设计.md` 中 v2.0 的原型复刻描述；详细页面设计与执行见 `docs/Web-UI-v2.1-设计与执行方案.md`。

---

## Brand

* **核心定位**：**数字典藏与历史长河的图书馆 (A Digital Home for Curious Minds & A Kind of Library)**。
* **视觉美学**：**现代独立出版物 / 典藏报刊风 (Modern Broadsheet Editorial)**。保留深栗红、金驼细线、象牙暖纸和清楚的排版层级，服务于归档、排障、检索与阅读；不为复刻样张添加期号、栏目号、口号或虚构策展内容。
* **高对比度承诺**：
  * 文字绝不使用低对比度的浅灰或发虚发淡的过渡色；正文、标题与列表全部采用**深沉浓郁的炭墨黑（`#12100E`）**，彻底保障全天候阅读的高对比度与舒适度。
* **避免**：
  * 营销式大留白与虚浮的大阴影。
  * 刺眼的纯白背景与死黑（OLED 刺眼黑）。
  * 在竖向漫画/长图阅读时使用粗厚顶部遮挡条抢占纵向视口。
  * 用低对比度、过小字体、隐形裁切或没有真实数据的按钮换取视觉整齐。

## Product goals

- 首屏明确页面目的、当前状态与下一步主操作；使帖子标题、任务故障原因和阅读内容优先于装饰与内部字段。
- 保留现有归档、检索、阅读、导出、审核与配置能力；不把样张中的“热门”“精选”“收藏”当作已实现能力。
- 成功信号：各关键路由在桌面与 390px 手机宽度均可读、可操作；长中日英标题可辨作品与话数；错误和部分完成状态可解释。

## Personas and jobs

- **归档管理者**：查看队列与异常、重试任务、维护系列和导出；需要准确状态、可追溯诊断与安全操作。
- **内容读者**：查找帖子、看漫画或长文、在楼层间跳转；需要完整标题、连续阅读和明确的图片状态。
- 同一用户可能在两种任务间切换，因此阅读页面减少管理字段，管理页面保留可展开的技术详情。

## Information architecture

- 保留现有路由；侧栏可按内容、工作、系统轻量分组，不改变地址。详情页使用返回/面包屑、对象标题、状态和主操作。
- 控制台是状态与最近内容入口；帖子/论坛以标题为主；任务以诊断与动作优先；知识检索默认聚焦搜索，索引管理为次级区域。`/forums` 在侧栏显示为“归档统计 / Archive Statistics”；`/exports` 保留为独立导出路由。完整路由规格见 v2.1 方案第 3 节。
- 控制台以紧凑索引展示各有归档内容分区的真实帖子数，并可直达对应的帖子列表；任务中心的“闲时任务”选中态与状态筛选选中态各自保持，切换一方不取消另一方的高亮。

## Design principles

1. **真实内容优先**：统计只标记实际计量对象，标签只表示真实排序或筛选；加载时不展示样张数字与虚构文章。
2. **标题优先**：先给标题宽度和两行阅读空间，再安排 TID、时间、标签、路径等次级字段。
3. **状态可行动**：错误先说明问题与下一步，技术原文保留在可展开详情；部分完成不得伪装成完成。
4. **裁剪不丢信息**：页面不能靠 `overflow-x:hidden` 遮掉操作或列；触屏和键盘也要能读完整标题。

---

## Visual language

### Color tokens

以下语义色延续纸墨视觉，由 `c/src/index.css` 管理；实际文本、徽章、边框须逐个验证对比度：

| 语义变量 | Light 浅色纸墨 (`:root`) | Dark 深茶黑夜 (`.dark`) | 视觉用途 |
| :--- | :--- | :--- | :--- |
| `--background` | **`#FFFDF4`** (温润典藏米白纸感) | **`#120E0C`** (深烘焙茶褐黑) | 整个视口底色 |
| `--foreground` | **`#12100E`** (浓郁深炭墨黑) | **`#F5EFEB`** (温暖象牙白字) | 正文、标题高对比度主色 |
| `--card` | **`#FFFDF8`** (微温纸张) | **`#1A1412`** (深色卡片底色) | 卡片、表格、浮层背景 |
| `--card-highlight`| **`#FAF3E3`** (柔和暖杏纸色) | **`#231915`** (深色突出底色) | 4 张顶部数据大卡、通告栏底色 |
| `--primary` | **`#682516`** (实心深印朱红) | **`#B84226`** (亮朱红微光) | 侧边栏激活项实心底色、分类下划线、主操作按钮 |
| `--border` | **`#D1BD98`** (典藏金驼细分割线) | **`#4B3629`** (深色暖褐细边框) | 卡片、表格边框 |
| `--border-subtle` | **`#EAE0D0`** (极淡纸质纤维线) | **`#241C17`** | 表格内行间分割线 |
| `--muted` | **`#EEE6D8`** (浅米灰) | **`#201815`** | 表头底色、次级徽章底色 |
| `--muted-foreground`| **`#5C5248`** (清晰深茶灰) | **`#A6998D`** | 副标题、时间戳、次级参数（高对比度） |
| `--destructive` | **`#C23824`** (珊瑚深朱红) | **`#EF4444`** | 删除、告警、异常状态 |

---

### Typography

针对论坛中存在大量的**中文、英文、日文（假名与和制汉字）混排**场景，配置三轨无缝回退字体族：

1. **界面与数据无衬线栈 (`font-sans`)**：
   * `Geist`, `Inter`, `-apple-system`, `PingFang SC`, `HarmonyOS Sans SC`, `Microsoft YaHei`, `Hiragino Sans`, `Yu Gothic`, `Noto Sans CJK SC`, `Noto Sans CJK JP`, sans-serif.
   * 用于侧边栏、控制台、数据表格、管理配置；确保英数 x-height 与中日文平衡，标点挤压得当。
2. **编辑标题栈 (`font-display`)**：
   * `Avenir Next`, `PingFang SC`, `Hiragino Sans GB`, `Noto Sans CJK SC`, sans-serif。界面页标题可使用 600–700 字重；帖子列表标题改用较轻的无衬线字形，与数据列保持协调。
   * 控制台主标题使用更大的字号和衬线字体，作为页面唯一的视觉主标题；其他界面标题继续使用统一的无衬线标题栈。
   * 导航不使用衬线字体。阅读器保留独立的 `font-serif` 选项及用户保存的字体设置。
3. **等宽数据监控栈 (`font-mono`)**：
   * `JetBrains Mono`, `Geist Mono`, `SF Mono`, Menlo, Monaco, Consolas, monospace.
   * 用于 TID 编号、Job ID、日志时间戳、版本号、向量检索得分；数据对齐工整。

---

### Motion

拒绝廉价缓慢的过渡，采用**物理纸墨与机械阻尼曲线**：

* **微弹展开曲线**：`cubic-bezier(0.16, 1, 0.3, 1)`（耗时 200ms ~ 300ms）。
  * 应用于全屏阅读模式展开、侧边栏折叠、弹窗浮层浮现。
* **状态与按钮微弹**：`cubic-bezier(0.34, 1.56, 0.64, 1)`（耗时 180ms ~ 220ms）。
  * 应用于任务完成 Badge 状态流转、主按钮点击。
* **物理触觉反馈**：
  * 卡片与表格行点击（Active）：`.press-feedback` 具有微小下沉回馈 `scale(0.996)`，提供实体操作感。

---

## 5. 页面与组件架构规范

### 5.1 全局导航与页面骨架
* 桌面侧栏使用暖纸底与细金分割线，激活项为深栗红实心块；保留品牌、导航及中/英和日夜切换。寄语若保留只在展开侧栏出现一次，不挤占内容。
* 桌面侧栏收起为 64px 时以单字母品牌标识代替完整文字；语言、主题控件纵向排列并保持在侧栏边界内。
* 低于 768px 侧栏默认收起为可关闭抽屉，不固定占据手机半屏。页面级内容不得被侧栏或隐藏溢出遮掉。
* 非首页统一页头：H1、对象/用途说明、状态、主操作；危险操作降级为次级菜单或独立区。详情页显示返回路径。

### 5.2 内容与任务优先控制台
* 以紧凑页头、真实归档统计、队列状态/待处理异常和通栏最近归档组成首屏；四张统计卡使用相同边框、底色与数字层级，运行任务也以数字为主，不让卡片标签断成竖排。
* 最近归档采用通栏索引表，标题优先，版块、回复数和收录日期为次级列；标题最多两行，完整标题仍可通过链接与悬停提示读取。保留版块筛选；热门、趋势、编辑精选只有在各自数据与排序语义成立后才出现。
* 帖子与任务的批量操作位于表格上方的固定高度工具行，右侧对齐；勾选前后表格、筛选和页头的纵向位置必须保持不变。窄屏工具行可横向滚动。
* 不显示 `ISSUE #TID`、`SECTION 01`、四词口号、固定英文散文、火漆章、重复金句或无操作的 `SAVE`。

### 5.3 独立全屏沉浸阅读器 (ThreadReader & Zen Mode)
* **共同交互**：帖子详情点击“沉浸阅读模式”或按 `R` 进入，按 `Esc` 退出；隐藏侧栏和顶部 Header，不设置厚重顶栏。控制项集中于底部轻量浮动工具条，包含退出和楼层跳转。
* **漫画区 (图像优先)**：连续纵向阅读，画布横向铺满视口，图片默认 100% 宽并提供 50% / 75% / 100% 选项；楼层信息压缩为轻量分隔，避免楼层卡片和大块留白打断画面。漫画控制只提供图宽，不显示无关的字体和字号选项。
* **小说区 (长文优先)**：正文保持约 66–72 字符的居中阅读行宽，支持衬线/无衬线、字号缩放和舒适行距；楼层与时间信息作为弱化元数据。小说按分页载入时，底部工具条必须可前后翻页，并保留楼层跳转。
* **其他内容**：使用中等宽度的通用正文版心和楼层跳转，不强行套用漫画满宽或小说字体控件。

### 5.4 标题列表、表格与面板
* 表格可沿用暖米灰表头与金色细线，但各页必须单独定义主列、次列和响应式隐藏顺序。帖子列表不显示独立 TID 列；帖子/系列标题左对齐、默认两行裁剪；桌面 1280px 左右标题目标至少 320px，变窄时改布局而非压到无法辨认。
* 通过焦点可达的展开或详情页提供完整标题；鼠标悬停 `title` 只是补充。路径、URL、时间戳与 JSON 不得反向挤占标题宽度。
* 手机列表转为标题优先的卡片行；允许明确标示的容器内横向滚动，但不允许页面级隐形裁切。现有列配置须提供可恢复的推荐布局。
* 作品系列与论坛漫游的连续条目使用清晰的暖金分隔线和轻微交替底色；审核工作使用“对象与核对线索—操作”的待办条目，操作始终在可视区内，相似候选与合并目标按需展开。

## Components

- 统一页面页头、状态/错误卡、空状态、标题单元、列表卡、数据表、操作按钮与阅读工具条。优先复用已有 `Layout`、`Badge`、`PaginationControls`、`TableLayoutEditor` 和 `ThreadReader`，只为重复的界面责任增加共用组件。
- Token 归 `c/src/index.css`；旧 `styles.css` 与各页样式逐步收敛，避免全局表格规则覆盖业务页列宽。

---

## Accessibility

1. **色彩对比度**：正文达到 WCAG AA 4.5:1，大字与控件边界至少 3:1；逐个检查错误卡、状态徽章与帮助文字，不凭基础文字 token 推断整个页面合格。
2. **键盘导航**：
   * 侧边栏折叠/展开：快捷键 `[`
   * 沉浸阅读模式进入/退出：快捷键 `R` / `Esc`
   * 表格分页与跳转支持 `Enter` 提交。
3. **动效自适应**：
   * 系统开启 `prefers-reduced-motion: reduce` 时，自动关闭弹簧过渡与缩放动效，降级为即时切换。

## Responsive behavior

- PC/宽屏以用户当前 Chrome 窗口的实际尺寸与分辨率为主验收基准；本轮实测窗口截图为 1163×768 像素。390×844、768×1024、1280×720、1440×900 作为辅助断点样本；检查侧栏、表格、阅读器、浮动控制条与键盘/触屏可达性。
- ≥1280px 保留必要的数据列；768–1279px 先折叠次级列；<768px 使用列表卡和抽屉导航。不得仅以“无页面横向滚动条”宣称响应式合格。

## Interaction states

- Loading 使用结构相近的骨架，无虚构数字；Empty 说明原因与下一步；Error 先说明故障与行动，再展开技术原文；Partial 明确完成与缺失；Disabled 解释条件；远端不可用与本地空结果分开表达。

## Content voice

- 简体中文界面优先使用清楚、短句的任务语言；内部代码名仅在技术详情中出现。状态名称与动作名称跨列表、详情、通知保持一致；精确时间和原始错误可复制，不强迫在紧凑列表中完整显示。

## Implementation constraints

- React/TypeScript/Vite、现有 API、路由、i18n 和主题为当前边界；前端源码改动后运行 `cd c && npm run build`，并对本地页面做视觉复查。构建成功不等于视觉或真实服务验收。
- 执行依赖顺序、页面验收、风险与文件范围见 `docs/Web-UI-v2.1-设计与执行方案.md` 第 4–5 节。

## Open questions

- [ ] 首页是否有可靠的阅读进度供“继续阅读”使用？负责人：产品/数据；影响：首页入口文案。
- [ ] 收藏、热门和编辑精选是否有真实数据契约？负责人：产品/数据；影响：相应按钮和标签是否出现。
- [ ] 深色模式和 `/forums` 的当前视觉基线待逐页检查。负责人：前端实施；影响：最终验收，不阻挡浅色页面方案。
