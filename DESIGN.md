# Yamibo Web 设计契约

## Source of truth

- 状态：Active
- 最近更新：2026-09-22
- 主要界面：`/chat` 对话工作区、`/settings` 常用设置，以及 `/settings/advanced` 高级设置。
- 依据：`c/src/pages/Chat.tsx`、`c/src/components/chat/`、`c/src/styles/chat.css`、`c/src/pages/Settings.tsx`、`c/src/styles/settings.css`、`c/src/context/I18nContext.tsx`。
- 本文件记录产品与视觉决策；实现细节仍以组件与样式源码为准。

## Brand

- 气质：安静、直接、可信，服务于论坛内容整理而不是展示模型能力。
- 信任信号：显示当前会话、执行状态、工具阶段和可追溯的结果；有副作用的操作必须保持审批入口。
- 避免：营销式大留白、重复状态、把设置访问令牌或内部协议术语放进普通对话流程，以及把只对特定后端有效的字段永久铺满页面。

## Product goals

- 让同一屏看到更多历史消息和当前执行进度。
- 用紧凑的会话栏、标题栏和输入区保留上下文，同时保证全文阅读和滚动稳定。
- 让只读查询、归档任务和工作文件的关系容易理解。
- 失败、等待审批和后台运行状态必须可见且可恢复。
- 让常用设置保持短而可理解；运行协议、诊断参数和部署状态通过独立高级界面渐进披露。
- 设置验证只保护管理面；验证结果覆盖同一浏览器会话，不把原始令牌保存在 Web Storage。

## Personas and jobs

- 唯一用户：项目维护者本人，熟悉 Yamibo 业务和任务状态，需要高频查看、整理和归档论坛内容。
- 访问环境：桌面浏览器为主，偶尔使用窄屏设备；站点部署在受保护的个人环境中。
- 设置任务：调整日常归档、模型、导出和后台任务行为；排障时再进入高级设置查看运行协议与部署状态。

## IA

- 左栏：会话列表、搜索和新建会话。
- 中栏：当前会话标题、状态、消息时间线和输入区。
- 右栏：可选的请求记录与工作文件面板；不抢占默认阅读空间。
- 会话消息是主叙事；思考、工具调用和运行详情默认收纳在可展开区域。
- `/settings`：常用设置，仅包含归档下载、AI 与聊天、导出、后台任务及相关功能入口。
- `/settings/advanced`：Worker 调度、RAG 索引、维护诊断、兼容服务和只读部署状态。
- 标题词典、表格布局、账号与代理池属于各自功能域，不作为常用运行设置平铺。

## Design principles

1. 内容优先：减少装饰和重复说明，把空间留给消息。
2. 状态就近：运行状态贴近当前会话，不要求用户切换页面寻找。
3. 渐进披露：摘要默认简洁，工具参数、结果和文件详情按需展开。
4. 可逆操作：停止、审批、重命名和删除保持明确的全行命中区域与反馈。
5. 诚实边界：界面不暗示通用系统权限；访问控制由服务端策略负责。
6. 条件披露：Hermes、RAG 等字段仅在对应能力启用时展开；无效或代码生成的参数不得伪装成可编辑设置。
7. 状态分离：明确区分已保存值、运行中生效值、环境变量锁定和等待重启状态。

## Visual language

- 沿用项目现有主题变量、边框和强调色，不新增独立颜色体系。
- 采用 8px 基础间距、低圆角和轻边框；层次主要通过留白、字号和背景色区分。
- 正文使用 13px 左右的紧凑阅读尺寸，辅助状态使用 10–11px；保留足够行高和对比度。
- 用户消息使用轻量气泡，助手消息保持连续正文宽度；工具与思考信息使用低对比度折叠面板。

## Components

- `ChatSessionList`：紧凑、可搜索、完整行可点击的会话导航。
- `ChatTranscript`：唯一主要滚动区，维护自动跟随与手动滚动状态。
- `ChatMessageView`：用户、助手、工具消息的统一语义与 Markdown 呈现。
- `ChatReasoning` / `ChatToolEvent`：默认收纳执行细节，展开后可滚动查看。
- `ChatComposer`：单一输入区，Enter 发送，Shift+Enter 换行，运行中支持排队或停止。
- `ChatLocalPanel`：按需打开的本地运行和文件上下文。
- `SettingsAccessGate`：设置管理验证；令牌只用于换取 HttpOnly 会话 Cookie，不进入对话流程。
- `SettingsSection`：分区 dirty 状态、字段级帮助、来源与生效状态、就地保存反馈。
- `AdvancedSettings`：高级二级路由；内部调度参数、诊断项和部署信息按主题分组。
- `DeploymentStatus`：只读显示配置来源、锁定项、运行值和等待重启项，不回显秘密。

## Accessibility

- 保持原生按钮、输入框、`details` 和 `role=status` 语义；图标按钮必须有 `aria-label`。
- 所有交互控件保留可见的 `:focus-visible` 轮廓，禁用状态不能误导为可操作。
- 文本、状态色和边框满足现有主题的对比度；不以颜色作为唯一状态信息。
- `prefers-reduced-motion: reduce` 下关闭过渡、滚动动画和脉冲效果。
- 设置字段使用真实 `label` 与输入控件关联；验证、保存和会话失效消息通过 `role=status` / `role=alert` 暴露。

## Responsive

- 桌面：会话栏约 196px；工作面板打开时约 270px，中央内容自适应。
- 中等宽度：隐藏非必要状态元数据，工作面板改为右侧覆盖层。
- 窄屏：会话列表改为覆盖层，工作面板占满宽度；消息、输入区和按钮仍保持可触控尺寸。
- 只有时间线滚动，侧栏和工作面板各自滚动，避免嵌套页面滚动抢夺输入焦点。
- 设置页在 900px 以下改为单列；二级导航与保存操作保持在内容之前，按钮触控高度不低于 40px。

## Interaction states

- 空会话：显示简短说明和三个可直接填入输入框的示例。
- 加载/重连：状态条保留在消息区顶部，不改变输入区位置。
- 工具调用/等待审批：显示当前阶段；审批操作保持显眼并可重复查看。
- 失败：保留错误和已完成的历史消息，提示下一步检查配置或重试。
- 后台运行：运行记录进入工作面板，关闭页面后可由现有恢复机制继续。
- 设置未验证：显示独立管理验证界面，明确它不是模型 API Key；验证后显示“本次浏览器会话”状态和退出入口。
- 设置会话失效：保留当前内存草稿，覆盖显示重新验证；验证成功后由用户再次触发保存，不自动重放写请求。
- 设置刷新：存在未保存改动时先确认；保存成功或失败反馈出现在对应分区而非页面远端。
- 条件配置：当前后端或能力未启用时显示摘要与启用入口，不渲染无效控件。

## Content voice

- 使用简体中文、短句和具体动词；优先说“查找、整理、归档、查看”。
- 面向用户说明结果和下一步，不在主流程解释 Pydantic AI、MCP 或 Hermes 的内部实现。
- 统一称为“设置访问令牌”；普通对话页不索取、不展示，也不携带设置凭据。
- 登录说明使用“本次浏览器会话”“不会存入网页存储”等可验证表述，不使用含混的“永久记住”。

## Implementation constraints

- 复用现有 React 组件、主题变量、会话/运行 hooks 和 API 客户端，不改变业务权限与任务语义。
- 前端源码修改后必须运行 `cd c && npm run build`，因为生产页面使用 `src/yamibo_mcp/web/static/` 的构建产物。
- 设置访问令牌只换取 HttpOnly、Secure、SameSite=Strict 的签名会话 Cookie；Cookie 不保存原始令牌、不设置固定过期时间，设置写请求保持同源与 CSRF 防护。
- 设置鉴权与 embedded/Hermes 后端解耦，覆盖 `/api/settings` 与 `/api/settings/` 下的管理接口；普通列表页仅通过独立的只读布局接口读取非敏感显示偏好，对话 API 不携带设置会话凭据。
- `rag_min_chunk_chars`、`rag_max_chunk_chars` 在真实消费者接入前不提供编辑；chunker 版本由实现生成并只读展示。

## Open questions

- [ ] 生产站点是否另有反向代理认证；owner：部署维护者；影响：是否需要在设置会话之外增加整站访问控制。
- 是否需要在会话标题旁长期显示模型名称；当前先保持状态简洁，把模型配置留在设置页。
